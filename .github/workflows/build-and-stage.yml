name: Build and push image to the registry

on:
  push:
    branches: [develop]

permissions:
  contents: read
  packages: write
  actions: write

env:
  IMAGE_TAG: sha-${{ github.sha }}

jobs:
  build_and_push:
    name: Build and push images
    runs-on: ubuntu-latest

    steps:
      - name: Lowercase owner for GHCR
        run: echo "OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push orders-api
        id: orders
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/orders-service/Dockerfile
          target: prod-distroless
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/${{ env.OWNER_LC }}/orders-api:${{ env.IMAGE_TAG }}

      - name: Build and push payments
        id: payments
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/payments-service/Dockerfile
          target: prod
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/${{ env.OWNER_LC }}/payments:${{ env.IMAGE_TAG }}

      - name: Build and push orders tools
        id: orders_tools
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/orders-service/Dockerfile
          target: tools
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/${{ env.OWNER_LC }}/orders-tools:${{ env.IMAGE_TAG }}

      - name: Build and push payments migrations
        id: payments_migrate
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/payments-service/Dockerfile
          target: build
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/${{ env.OWNER_LC }}/payments-migrate:${{ env.IMAGE_TAG }}

      - name: Write release-manifest.json
        env:
          ORDERS_DIGEST: ${{ steps.orders.outputs.digest }}
          ORDERS_TOOLS_DIGEST: ${{ steps.orders_tools.outputs.digest }}
          PAYMENTS_DIGEST: ${{ steps.payments.outputs.digest }}
          PAYMENTS_MIGRATE_DIGEST: ${{ steps.payments_migrate.outputs.digest }}
        run: |
          jq -n \
            --arg commit "${{ github.sha }}" \
            --arg tag "${{ env.IMAGE_TAG }}" \
            --arg owner "${{ env.OWNER_LC }}" \
            --arg od "$ORDERS_DIGEST" \
            --arg otd "$ORDERS_TOOLS_DIGEST" \
            --arg pd "$PAYMENTS_DIGEST" \
            --arg pmd "$PAYMENTS_MIGRATE_DIGEST" \
            '{
              commit: $commit,
              image_tag: $tag,
              services: {
                "orders-api": {
                  image: ("ghcr.io/" + $owner + "/orders-api:" + $tag),
                  digest: $od
                },
                "orders-tools": {
                  image: ("ghcr.io/" + $owner + "/orders-tools:" + $tag),
                  digest: $otd
                },
                payments: {
                  image: ("ghcr.io/" + $owner + "/payments:" + $tag),
                  digest: $pd
                },
                "payments-migrate": {
                  image: ("ghcr.io/" + $owner + "/payments-migrate:" + $tag),
                  digest: $pmd
                }
              }
            }' > release-manifest.json
          cat release-manifest.json

      - name: Upload release manifest
        uses: actions/upload-artifact@v4
        with:
          name: release-manifest
          path: release-manifest.json

  deploy_stage:
    name: Deploy to stage
    if: github.ref == 'refs/heads/develop'
    runs-on: [self-hosted, stage]
    needs: build_and_push

    environment:
      name: stage
      url: http://localhost:8080

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node for post-deploy e2e
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Download release manifest
        uses: actions/download-artifact@v4
        with:
          name: release-manifest
          path: .

      - name: Check manifest
        run: |
          ls -la
          cat release-manifest.json

      - name: Read manifest
        run: |
          echo 'ORDERS_IMAGE<<EOF' >> $GITHUB_ENV
          jq -r '.services["orders-api"].image' release-manifest.json >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

          echo 'ORDERS_TOOLS_IMAGE<<EOF' >> $GITHUB_ENV
          jq -r '.services["orders-tools"].image' release-manifest.json >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

          echo 'PAYMENTS_IMAGE<<EOF' >> $GITHUB_ENV
          jq -r '.services["payments"].image' release-manifest.json >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

          echo 'PAYMENTS_MIGRATE_IMAGE<<EOF' >> $GITHUB_ENV
          jq -r '.services["payments-migrate"].image' release-manifest.json >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

      - name: Write deploy/.env.stage from Environment secrets
        working-directory: ${{ github.workspace }}
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          PAYMENTS_GRPC_PORT: ${{ secrets.PAYMENTS_GRPC_PORT }}
          PAYMENTS_GRPC_TIMEOUT_MS: ${{ secrets.PAYMENTS_GRPC_TIMEOUT_MS }}
          PAYMENTS_GRPC_BIND_URL: ${{ secrets.PAYMENTS_GRPC_BIND_URL }}
          DB_HOST_PORT: ${{ secrets.DB_HOST_PORT }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_EXPIRES_IN: ${{ secrets.JWT_EXPIRES_IN }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_BUCKET: ${{ secrets.AWS_BUCKET }}
          AWS_ENDPOINT: ${{ secrets.AWS_ENDPOINT }}
          CLOUDFRONT_BASE_URL: ${{ secrets.CLOUDFRONT_BASE_URL }}
          RABBITMQ_USER: ${{ secrets.RABBITMQ_USER }}
          RABBITMQ_PASSWORD: ${{ secrets.RABBITMQ_PASSWORD }}
          RABBITMQ_PREFETCH: ${{ secrets.RABBITMQ_PREFETCH }}
          RABBITMQ_URL: ${{ secrets.RABBITMQ_URL }}
          RABBITMQ_QUEUE_ORDERS: ${{ secrets.RABBITMQ_QUEUE_ORDERS }}
          RABBITMQ_QUEUE_DLQ: ${{ secrets.RABBITMQ_QUEUE_DLQ }}
          RABBITMQ_RETRY_COUNT: ${{ secrets.RABBITMQ_RETRY_COUNT }}
          RABBITMQ_RETRY_DELAY: ${{ secrets.RABBITMQ_RETRY_DELAY }}
          PAYMENTS_DB_HOST: ${{ secrets.PAYMENTS_DB_HOST }}
          PAYMENTS_DB_PORT: ${{ secrets.PAYMENTS_DB_PORT }}
          PAYMENTS_DB_USER: ${{ secrets.PAYMENTS_DB_USER }}
          PAYMENTS_DB_PASSWORD: ${{ secrets.PAYMENTS_DB_PASSWORD }}
          PAYMENTS_DB_NAME: ${{ secrets.PAYMENTS_DB_NAME }}
          MINIO_ROOT_USER: ${{ secrets.MINIO_ROOT_USER }}
          MINIO_ROOT_PASSWORD: ${{ secrets.MINIO_ROOT_PASSWORD }}
          MINIO_API_PORT: ${{ secrets.MINIO_API_PORT }}
          MINIO_CONSOLE_PORT: ${{ secrets.MINIO_CONSOLE_PORT }}
          PORT: ${{ secrets.PORT }}
        run: |
          set -euo pipefail
          mkdir -p deploy
          python3 <<'PY'
          import os
          keys = [
              "DB_HOST", "DB_PORT", "PAYMENTS_GRPC_PORT", "PAYMENTS_GRPC_TIMEOUT_MS",
              "PAYMENTS_GRPC_BIND_URL", "DB_HOST_PORT", "DB_USER", "DB_PASSWORD", "DB_NAME",
              "JWT_SECRET", "JWT_EXPIRES_IN", "AWS_REGION", "AWS_ACCESS_KEY_ID",
              "AWS_SECRET_ACCESS_KEY", "AWS_BUCKET", "AWS_ENDPOINT", "CLOUDFRONT_BASE_URL",
              "RABBITMQ_USER", "RABBITMQ_PASSWORD", "RABBITMQ_PREFETCH", "RABBITMQ_URL",
              "RABBITMQ_QUEUE_ORDERS", "RABBITMQ_QUEUE_DLQ", "RABBITMQ_RETRY_COUNT", "RABBITMQ_RETRY_DELAY",
              "PAYMENTS_DB_HOST", "PAYMENTS_DB_PORT", "PAYMENTS_DB_USER", "PAYMENTS_DB_PASSWORD",
              "PAYMENTS_DB_NAME", "MINIO_ROOT_USER", "MINIO_ROOT_PASSWORD", "MINIO_API_PORT",
              "MINIO_CONSOLE_PORT", "PORT",
          ]
          path = os.path.join("deploy", ".env.stage")
          with open(path, "w", encoding="utf-8") as f:
              for k in keys:
                  f.write(f"{k}={os.environ.get(k, '')}\n")
          st = os.stat(path)
          print(f"Wrote {path} ({st.st_size} bytes)")
          if st.st_size == 0:
              raise SystemExit("deploy/.env.stage is empty")
          PY
          test -f deploy/.env.stage
          ls -la deploy/.env.stage

      - name: Login to GHCR
        run: |
          for attempt in 1 2 3 4 5; do
            if echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin; then
              exit 0
            fi
            echo "GHCR login failed, retry ${attempt}/5"
            sleep $((attempt * 5))
          done
          exit 1

      - name: Deploy stage stack
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          test -f deploy/.env.stage
          export ORDERS_IMAGE="${ORDERS_IMAGE}"
          export ORDERS_TOOLS_IMAGE="${ORDERS_TOOLS_IMAGE}"
          export PAYMENTS_IMAGE="${PAYMENTS_IMAGE}"
          export PAYMENTS_MIGRATE_IMAGE="${PAYMENTS_MIGRATE_IMAGE}"

          docker compose \
            -p myapp-stage \
            --env-file deploy/.env.stage \
            -f deploy/compose.stage.yml \
            pull

          docker compose \
            -p myapp-stage \
            --env-file deploy/.env.stage \
            -f deploy/compose.stage.yml \
            down --remove-orphans

          docker compose \
            -p myapp-stage \
            --env-file deploy/.env.stage \
            -f deploy/compose.stage.yml \
            up -d

      - name: Show running containers
        run: docker ps

      - name: Post-deploy health check
        run: |
          for i in {1..20}; do
            if curl --fail http://localhost:8080/health; then
              exit 0
            fi
            echo "Health check failed, retry $i/20"
            sleep 3
          done
          echo "Service did not become healthy in time"
          docker ps
          docker logs myapp-stage_orders-api_1 --tail 200 || true
          exit 1

      - name: Stage main flow e2e
        env:
          BASE_URL: http://localhost:8080
        run: node scripts/e2e-main-flow.mjs
